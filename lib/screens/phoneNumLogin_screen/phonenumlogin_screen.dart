// ignore_for_file: unused_local_variable, prefer_const_constructors, duplicate_ignore

import 'package:flutter/material.dart';
import 'package:flutter_application_assessment1/screens/phoneNumLogin_screen/components/body.dart';

class PhoneNumLoginScreen extends StatelessWidget {
  const PhoneNumLoginScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Body()),
    );
  }
}
